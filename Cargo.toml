[package]
name = "todo-app"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = {version = "0.28.1", features = ["persistence"]}
serde = {version = "1.0", features = ["derive"]}
serde_json = "1.0.120"
